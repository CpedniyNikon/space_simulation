name: space_simulation
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flame: ^1.0.0
  flame_audio: ^1.0.0
  matrices: ^1.2.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/space.png
    - assets/images/SunRed.png
    - assets/images/appendButton.png
    - assets/images/Terran.png
    - assets/images/Lava.png
    - assets/images/Ice.png
